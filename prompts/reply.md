<!--
Style guide for the Reply Call. This is prepended to the routing stage's
own reply_system_prompt, so it applies to every reply regardless of what
the routing stage decided to write for that specific question.

Add conventions here as you notice things you want changed — tone, formatting,
units, how to handle empty results, etc. Leave it blank to have no fixed
style layer at all.
-->
